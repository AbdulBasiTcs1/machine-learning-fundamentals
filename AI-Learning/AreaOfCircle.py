from math import pi
radius = int(input("Enter the radius of circle : "))
# To calculate area of circle we need pi and radius formula = Area = pi x radius(square)
print("The Area of circle with radius ",str(radius)," is : ",str(pi*radius**2)) 
